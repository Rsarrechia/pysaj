# Runtime dependency. The authoritative dependency list now lives in
# pyproject.toml; this file is kept for convenience.
#
#   Install the library:            pip install .
#   Install with test extras:       pip install .[test]
aiohttp>=3.8
